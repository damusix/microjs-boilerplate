<navigation>

    <a each={ opts.links } class={ active: selected } href={ href } alt={ alt || title || text } title={ title || alt || text }>{ text }</a>

    <script>
        console.log(this)
    </script>
</navigation>
